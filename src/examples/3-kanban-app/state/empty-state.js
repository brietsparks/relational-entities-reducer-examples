import { emptyState as entities } from '../entities';
import { VIEW_BOARD } from '../constants';

export const filteredByUser = null;
export const contentView = VIEW_BOARD;
export const viewedTaskId = null;

export default {
  filteredByUser,
  contentView,
  viewedTaskId,
  entities,
}
